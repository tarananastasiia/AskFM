@model AskFM.Models.Question
@{
    ViewData["Title"] = "Добавление вопроса";
}
@if (User.Identity.IsAuthenticated)
{
    <form asp-action="create" asp-controller="home">
        <div class="form-group">
            <label asp-for="Text" class="control-label">Вопрос</label>
            <input type="text" asp-for="Text" class="form-control" />
        </div>
        @*<div class="form-group">
                <label asp-for="Answer" class="control-label">Ответ</label>
                <input type="text" asp-for="Answer" class="form-control" />
            </div>*@
        <div class="form-group">
            <input type="submit" value="Отправить" class="btn btn-default" />
        </div>
    </form>
}

