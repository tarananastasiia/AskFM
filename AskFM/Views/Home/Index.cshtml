@model AskFM.Models.Question
@{
    ViewData["Title"] = "Добавление вопроса";
}

@*@if (User.Identity.IsAuthenticated)
{
    <form asp-action="create" asp-controller="home">
        <div class="form-group">
            <label asp-for="Questions" class="control-label">Вопрос</label>
            <input type="text" asp-for="Questions" class="form-control" />
        </div>
        <div>
            <input type="checkbox" id="check2"><label>Анонимно</label><br>
        </div>
        <div class="form-group">
            <input type="submit" value="Отправить" class="btn btn-default" />
        </div>
    </form>
}
else
{
    <form asp-action="create" asp-controller="home">
        <div class="form-group">
            <label asp-for="Questions" class="control-label">Вопрос</label>
            <input type="text" asp-for="Questions" class="form-control" />
        </div>
        <div class="form-group">
            <input type="submit" value="Отправить" class="btn btn-default" />
        </div>
    </form>
}*@